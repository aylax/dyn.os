# ------------------------------------------------------------------------------
# Dynamic Link Stage
# ------------------------------------------------------------------------------
FROM aylax:lynn-basic as builder

WORKDIR /app/lynn
COPY .cargo .cargo
COPY Cargo.toml Cargo.toml
RUN mkdir src && echo "fn main () {}" >> src/main.rs
RUN cargo build --release
RUN rm -f target/release/deps/lynn*

